<image class="load-failure-image" mode="aspectFill" src="{{imgsrc}}" hidden="{{hidden}}" bindtap="clickRetry"/>
<view class="girl-gallery">
    <view class="girl-item" wx:for="{{images}}">
        <image src="{{item}}" mode="widthFix" data-src="{{item}}" bindtap="previewImage"/>
    </view>
 </view>



